# Vite environment (local overrides)
# Copy to .env.local for local development.

# For local AI proxy testing, point this to a running backend endpoint.
# Leave empty to fallback to local draft generation.
VITE_AI_GENERATOR_ENDPOINT=

